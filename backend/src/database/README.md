# How to migrate database to local or empty database

Follow these steps if:
1. Database is on local machine
2. Database is up but still empty

Steps:
1. Create database (asumming there is no database)
2. Connect backend to database
3. run `sh run-drizzle-push.sh`
4. Done